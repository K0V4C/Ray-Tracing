
#[derive(Default)]
pub struct fPixel {
    pub red: f32,
    pub green: f32,
    pub blue: f32,
    pub alpha: f32
}

#[derive(Default)]
pub struct uPixel {
    pub red: u8,
    pub green: u8,
    pub blue: u8,
    pub alpha: u8
}

